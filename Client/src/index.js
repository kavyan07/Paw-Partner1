import React from 'react';
import ReactDOM from 'react-dom';
import './styles.css'; // Ensure this line matches the path to your CSS file
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
